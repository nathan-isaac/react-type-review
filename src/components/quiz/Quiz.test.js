// it shows all our cards and their options

// lets us filter the cards based on right/wrong

// lets us score the quiz

// tracks which cards are trouble cards

// lets hide/show font names

// it shuffles cards

// lets us reset the quiz to try again

it("do something", () => {
  expect(true).toBe(true);
});
